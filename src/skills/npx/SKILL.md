---
name: npx
description: "Run npm executables via npx: package scripts, CLI tools, and one-off commands."
tags: [npx, npm, node, scripts, cli]
---

Use NPX tooling when the user asks to run a package or command without installing it globally.
Preferred sources of truth:
- local registered tools: npx_run
Every NPX action must map directly to one of those tools or another validated npx workflow.
