# TSC skill

## What it does
The tsc skill covers TypeScript check, compile, and watch workflows.

## Tools
Key tool:
- `tsc_command`

## Example usage
Type-check a project:
```text
Run TypeScript checks using tsconfig.json.
```

Compile a project:
```text
Compile this TypeScript project and summarize emitted JS files and type errors.
```
